session_id:ea20a17a-96c1-4d7b-bcff-575f09d73fd0

# 新需求-PC端改造需求

## 一、聊天

用户列表优先显示用户名，若无用户名才显示PeerID。
新增用户聊天，不要有一个空消息。很丑。

## 二、发现中心

重构一下页面，美化页面。

## 三、设置

页面重构，美化页面。

## 四、网络日志

1、设置里的“网络数据日志记录”如果开启，则显示“网络日志”菜单，否则不显示这个菜单。
2、网络数据日志，显示的是 request-response 改造之后的网络日志，应有一个 request 字段和 response 字段，分别显示一个网络请求的request和response。
3、表格字段：时间、对端、业务类型、request、response、数据大小。

## 五、通话

左侧菜单去掉“通话”菜单。
和用户的通话，应该发生在“聊天”内容区域。