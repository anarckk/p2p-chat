session_id:88197dfd-506c-4f10-958e-7abb1def2016

五段式通信协议，要封装起来，应用于其他通信业务场景。

针对其他业务场景，进行一些针对性适配，比如传输头像的时候，设计头像id，
传输用户名的时候，设计用户名id

传输在线设备列表的时候，传输在线设备PeerID列表的唯一标识。这个唯一标识，唯一标识并不是全宇宙唯一，而是用户局部唯一。是这个用户设备的唯一标识。