# Prompt

`
当前的 vue-peerjs-util/src/App.vue 迁移到子路由页面去，子路由: /test

再弄两个子路由, 分别是 /wechat  /center

/center 是发现中心，发现中心的作用是，所有的peer端都寻找发现中心，根据发现中心，找到其他设备PeerId

/wechat 是聊天应用，聊天应用两栏布局，左侧是发现中心告知本设备的所有对端设备，点开之后，右侧是和这个对端设备的聊天窗口。
点击直接发送消息。支持移动端。
或者你可以自己设计一下，要美观现代风格，最好用上 vue-antd 组件

需求看起来很复杂，很乱，其核心就一点，我希望 /wechat 设备间实现自动发现，用户可以通过系统自动发现的所有在线设备，然后选择在线的设备进行聊天。
那如果已经聊天过的设备下线了，我这边也不删除，保留着，如果消息发送失败，下次他上线了能自动发送消息给他。

如果说，对端设备直接和我发起通信，那么直接出现在我的聊天列表里，等待我点击聊天列表，在聊天窗口里就能和他通信。

还有一个设备名称的问题，如果都用UUID那么很难辨认对端设备，最好是首次进入 /wechat 的时候，能够要求用户输入用户名，并上传头像文件。然后在好友列表里的时候，能够显示头像、用户名、在用小字显示对端设备唯一标识UUID（peerId），如果用户不上传头像也可以，用 antd 默认头像。

通信的基础组件就是 vue-peerjs-util/src/util/PeerHttpUtil.ts ，这个我测试过了，能用。挺稳定的。

你分析一下我的需求，缕一缕思路，做一下plan，然后再进行开发。
`